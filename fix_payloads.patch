<<<<<<< SEARCH
  const handleFetchAttendance = async () => {
    if (!attendanceDate) return;
    setLoading(true);
    setError(null);
    try {
      const resp = await apiService.earnings.fetchAttendance({
        gardenid,
        date: attendanceDate
      });

      const newInputs: Record<string, { extra: string; type: string }> = {};
      const newPresence: Record<string, string> = {};

      if (resp && resp.data) {
        resp.data.forEach(item => {
          newPresence[item.labourerid] = item.status;
          newInputs[item.labourerid] = { extra: item.extra.toString(), type: item.type };
        });
      }
=======
  const handleFetchAttendance = async () => {
    if (!attendanceDate) return;
    setLoading(true);
    setError(null);
    try {
      const resp = await apiService.earnings.fetchAttendance({
        labourerids: labourers.map(l => l.labourerid),
        date: attendanceDate
      });

      const newInputs: Record<string, { extra: string; type: string }> = {};
      const newPresence: Record<string, string> = {};

      if (resp && Array.isArray(resp)) {
        labourers.forEach((l, index) => {
          newPresence[l.labourerid] = resp[index] ? "present" : "-";
          newInputs[l.labourerid] = { extra: "", type: "hr" }; // Need to fetch existing extra? The array is just boolean `[true, false]`, so extra might not be returned by fetch-attendance endpoint based on its source.
        });
      } else if (resp && resp.data) {
        resp.data.forEach((item: any) => {
          newPresence[item.labourerid] = item.status;
          newInputs[item.labourerid] = { extra: item.extra?.toString() || "", type: item.type || "hr" };
        });
      }
>>>>>>> REPLACE
